spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/betsafe_test
    username: betsafe
    password: betsafe_secret

  kafka:
    bootstrap-servers: localhost:9092

betsafe:
  schema-registry:
    url: http://localhost:8081
  minio:
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin_secret
    bucket-name: betsafe-events-test
